import type { FC } from 'react';
import Typography from '@mui/material/Typography';

export const ObjectViewNull: FC = () => (
  <Typography component="span" color="gray.500" fontFamily="monospace">
    null
  </Typography>
);
