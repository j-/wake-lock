import Typography from '@mui/material/Typography';
import type { FC } from 'react';

export const ObjectViewUnknown: FC = () => (
  <Typography component="span" color="gray.500" fontStyle="italic">
    unknown
  </Typography>
);
