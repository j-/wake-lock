import Typography from '@mui/material/Typography';
import type { FC } from 'react';

export const ObjectViewUndefined: FC = () => (
  <Typography component="span" color="gray.500" fontFamily="monospace">
    undefined
  </Typography>
);
